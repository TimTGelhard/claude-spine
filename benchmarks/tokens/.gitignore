# Harness output — regenerated by each `./run.sh`. Not committed.
# `aggregate.py` reads results.jsonl and writes REPORT.md (which IS tracked
# so a fresh clone sees the latest published numbers).
results/
