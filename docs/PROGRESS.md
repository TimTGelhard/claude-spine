# Progress — claude-spine

> Live pointer to where work resumes. Updated at every `/done`.
> Claude reads this first at the start of every session (via the `op-spine-active` skill).
>
> This file does NOT inventory features (see `CHANGELOG.md` for shipped work) or list every bug (see `FIXES.md` for the open queue).
> Its job is to answer: *what's the next session, what does it need, what's blocking it?*

<!-- ⚠️ DO NOT REFORMAT THE NEXT 6 LINES (or the **Session** bullet below). They are parsed by `spine-writeback.sh` via a regex that expects the literal `- **Section**:` / `- **Session**:` shape with backticks around the name. If you swap the bold for italics, drop the backticks, change the bullet style, or move these bullets to a different sub-heading, the Stop-hook heartbeat will silently no-op — your session activity won't be logged. -->

## Active section

- **Section**: `apply-03` — **not yet planned; run `/prep apply-03`** to choose + scope the next FIXES cluster. apply-02 is **done** (closed 2026-05-30 — A15 resolved: 7 harmful op-* trigger overlaps + 2 description-shape drift items fixed across 10 `description:` fields + 1 Sibling bullet). apply-01 **done** 2026-05-29 (A1 token-efficiency reframe).
- **Plan file**: none yet. `/prep apply-03` drafts `docs/plans/apply-03-<cluster>.md`. Candidate clusters by FIXES severity: **A16** (audit-infra bugs — A16.1/PF2 heartbeat-delta is the cheapest early-apply, it pollutes audit trails) · **A17** (install/test/docs — A17.1 `uninstall.sh` orphans 5/6 hooks is a real functional bug) · **A2** (personalization wiring) · **A12** (`/verify` capstone). The user picks at `/prep` (as with apply-02's A15).
- **Section status**: `not-started` — awaiting `/prep apply-03`. Apply-phase edit-rules still apply (edit-allowed against `skills/core/` for audit-routed clusters; no bundling; FIXES stays action-shaped).

## Active session

- **Session**: `none` — no active build session until `/prep apply-03` runs (the planning pass writes the section file + first session entry, then advances this pointer).
- **Status**: `awaiting /prep` — run `/prep apply-03` to choose the next FIXES cluster by severity and scope Session 1. No build until then.

## Last session outcome

**apply-02 Session 2 — done 2026-05-30; apply-02 section COMPLETE** (description-shape drift cleanups + A15 close, **no API spend**). Applied the last 2 of A15's clauses: **F4** op-curate-nudge's `description:` now leads with the `When the bucket loop is enabled (default-off since round 6),` gate (the default-off bucket loop made the old "fires for everyone" framing misleading — body loaded then exited); **F7** op-curate's `description:` trimmed of the apply/reject procedure + read-before-write recap, keeping what-it-does / triggers / diff-gate / bucket-only scope-refusal (the body's Flow steps 4–5 + the `Read-before-write — hard rule` section retain every mechanic — grep-confirmed). **Overlap re-check clean:** F4 narrows op-curate-nudge's firing (stays benign vs op-curate); F7 left op-curate's trigger phrases unchanged, and the `/curate` FP is already covered by S1's F5 carve-out on op-bucket-router. **F10/F11 open question → LEFT** (user-confirmed this session: they stay in audit-04 as polish, NOT folded — no-bundling per CLAUDE.md). **`FIXES.md` A15 → ✅ RESOLVED** (the F1–F9 enumeration essay compressed to one action-shaped bullet + pointer to audit-04 § Findings, rule 1); **deferred-benchmark note recorded** in the A15 entry (re-run warranted but blocked on audit-06's `claude -p` confound fix; `op-hooks.json` L4 expectation stale post-F2 → re-point or set `should_trigger:false` before any authorized re-run). **CHANGELOG** `[Unreleased]` Changed bullet added (newest-first, above the apply-01 A1 bullet). **Verify:** `tests/run.sh` 7 suites / 0 fail; concrete S2 checks all pass; API routing benchmark **not** run (deferred); no working-tree changes under `tests/skill-triggers/`. `git diff` scope: 2 SKILL.md *descriptions* (op-curate-nudge, op-curate) + `FIXES.md` + `CHANGELOG.md` + the 3 plan/progress docs — no SKILL.md *bodies*, no `chapters/`, no `tests/`. Pointer → apply-03 (needs `/prep`; next cluster by severity).

## Blockers

- **None.** apply-02 is complete (A15 resolved); the next section (apply-03) is unblocked but **unplanned** — run `/prep apply-03` to choose + scope the next cluster.
- **Carried constraint (not a blocker):** the **API routing benchmark stays deferred** — audit-04's `claude -p` routing-skill confound makes a re-run reproduce a 0–20 % TP artifact; the re-run becomes worthwhile only *after* audit-06 fixes the harness. A15's RESOLVED entry now records this plus the `op-hooks.json` L4 stale-eval to fix before any authorized re-run. apply-02 fixed routing using the audit-04 overlap matrix as the design-time proxy.
- **Remaining apply clusters (for `/prep apply-03+`, by severity):** A16 (audit-infra bugs; A16.1/PF2 heartbeat is the cheapest early-apply, it pollutes audit trails), A17 (install/test/docs; A17.1 — `uninstall.sh` orphans 5/6 hooks — is a real functional bug), A2 (personalization wiring), the A12 `/verify` capstone. Non-blocking candidate carried from apply-01: `aggregate.py` Totals row is sum-based / over-reports on unequal n (+94.2 % vs the true +76.6 %) — fold into a benchmark-hardening item.

## Next session reading list

The next section is **apply-03** — but it must be **planned first** (`/prep apply-03`); no build session is queued. The `/prep` pass chooses the next FIXES cluster by severity (A16 / A17 / A2 / A12 — see Blockers) and scopes Session 1. For the planning pass, read in order:

1. `FIXES.md` — the open `A##` clusters; pick the next by severity (as apply-01 chose A1 and apply-02 chose A15). A16/A17 are the audit-infra + install/test/docs follow-ups (A17.1 `uninstall.sh` orphans 5/6 hooks is a real functional bug); A2 is the personalization wiring; A12 is the `/verify` capstone.
2. `docs/PROJECT_PLAN.md` § Sections table + § Status log — confirm apply-02 `done`; add the apply-03 row when `/prep` picks the cluster.
3. `CLAUDE.md` — the apply-phase discipline (edit-allowed against `skills/core/` for audit-routed clusters; no bundling; FIXES stays action-shaped) + the relevant `op-*` planning skills (`op-prepare` runs the pass).

---

## Session log

Append-only. One line per session. Prune past ~30 entries by moving older lines to `docs/sessions-archive.md`.

### 2026-05-28
- Plan created. Audit sections 1–6 drafted. Section 1 fully detailed; sections 2–6 stubbed (per 05h: plan sections lazily before they start).
- Section 0 (`section-0-approach-feature`) inserted before audit-01. Building `op-approach` skill + `chapters/workflow/05k-work-shapes.md` as a pre-audit feature add so the spine carries work-shape-aware preparation discipline before the audit phase begins. Wires the cross-section coherence rule (audit-shape's hard rule) into the spine permanently, not only as a project-plan banner.
- Section 0 done. Shipped op-approach (~85 lines, router-shape) + 05k (~130 lines, catalog) + cross-references in 05h / op-prepare / INDEX + 11-edit count-claim sweep across CLAUDE.md / README.md / install.sh / op-welcome/SKILL.md / landing/index.html (23 op-* skills, 84 atomic chapters; resolves pre-existing `~80` tilde-claim — actual pre-feature was 83, post-feature exact 84). Audit-05 PF1 re-framed `blocking` → `info`. Pointer advanced to audit-01-architecture / Session 1.
- Meta-prep pass (audit-phase pre-flight protocol added to PROJECT_PLAN + audit-03..06 pre-flight pointers + audit-05 PF2–PF5 + section-0 stale status fix). Audit-phase restart requested 2026-05-28: discard the audit-01 + audit-02 runs that happened during the prep-pass / parallel-session window, revert section files + FIXES (A13 cluster + A14 removed) + PROGRESS pointer + PROJECT_PLAN Sections table to pre-audit-phase state. Audit phase resumes cleanly at audit-01 Session 1.
- Audit-01-architecture Session 1 done. 8 findings (0 blocking, 4 drift, 4 polish). New A13 cluster in `FIXES.md` consolidates F4 + F5 + F6 + F8 as one apply-section worth of one-line fixes (~30 min); polish (F1/F2/F3/F7) stays in the section file. PF1 partially overstated — three sweep residuals (README L14, README L135–143, RECONSTRUCTION L11). Pointer advanced to `audit-02-token-cost` Session 1 (stub — `/prep` needed first).
- Audit-02 `/prep` ran. Section file `docs/plans/audit-02-token-cost.md` re-detailed (Session 1 procedure populated with Path A (LC1 authorized) / Path B (static-only) branching at step 1 — the LC1 authorization gate). Cross-section notes from audit-01 (PF1 partial-overstatement methodology implication + F1 cost-data carry-forward) propagated forward. **Write-path workaround used:** the section file's canonical path matches the `**/*token*` deny rule (Read AND Bash both blocked, not just Read-glob as audit-05 PF5 originally framed), so Write enforced "Read first" and refused. Workaround: wrote to sibling staging path `docs/plans/audit-02-cost-section.md.staging` for the user to `mv` into place. Audit-05 PF5 should be re-framed during audit-05 (Read AND Bash blocked → in-band write deadlock when file already exists). Read-deny lift required before Session 1 can execute (cold-start needs to read the section file).
- Audit-02-token-cost Session 1 done. Path A executed (LC1 authorized). 6 findings (0 blocking, 5 drift, 1 polish). F0 LC1 headline: spine-on +75.9% input tokens vs spine-off on n=11 paired prompts (+28.6% cost/call, Sonnet 4.6, total LC1 spend ~$6.48). Headline contradicts naive "spine saves tokens" framing — reframing to "bounded per-load cost + routing accuracy" is the load-bearing A1 apply task. F2 (`questions-deep.md` L308-317 broken-cross-ref + dead-weight) added to A13 cluster as 5th drift item. F3 confirms all top-5 heaviest stacks in green zone (max 4.1% of Sonnet 200k). F5 captures mid-run `claude` CLI upgrade (2.1.153→2.1.154) that wiped spine-on phase; recovered via `--only-cond on` re-run; harness preflight (binary mtime check) added to A1 path. A1 extended with full results + reframing notes; A13 extended with F2. Read-only compliance verified by `git diff` against `8ad2c91`: mutations confined to `docs/plans/audit-02-token-cost.md` + `FIXES.md`. Read-deny on `**/*token*` was no longer active at Session 1 (lifted between /prep and execution — captured in cross-section notes for audit-05 PF5 reframe). Pointer advanced to `audit-03-personalization` (stub — `/prep` required first).
- Audit-03 `/prep` ran. Section file elaborated from 56-line stub into a fully-detailed Session 1 entry (field/consumer coverage audit, both directions: every field → a real consumer, every claimed `19g` consumer → a real read). Two decisions locked: **(1) consumer test** — a field is real if EITHER mechanically read (a script/skill greps + branches) OR instruction-honored (a chapter tells Claude to honor it + the field is in-profile); only "neither" is decorative (the standard A2.1/A2.2 were judged "wired" by). **(2) single session** with a documented clean split point if per-claim verification balloons. Cross-section notes carried forward: audit-01 **PF1 methodology** (grep-verify "shipped"/"Read by" claims, don't trust the prose) — load-bearing; audit-02 F2/F3 (don't re-find). Preliminary observation: template field-set ↔ `19g` row-set look aligned at ~41 (29 interview + 12 spine-defaults), so the audit's center of gravity is **consumer-claim verification**, not doc-row reconciliation. No `chapters/`/`skills/core/`/code/template edits; writes confined to the section file + this PROGRESS pointer refresh. Pre-flight rule 1 satisfied.
- Audit-03-personalization Session 1 done. 41 fields verified (29 interview + 12 spine-defaults — "~41" confirmed exact); **37 real (12 mechanical, 25 instruction), 4 decorative.** Decorative: Session shape (G1, = A2.4) + **new cluster** of 3 `Prep*` Planning fields (`procedure.md`/`prep.md` hardcode the caps instead of reading the profile → new **A2.5**). A2.3 found **stale** (D1 already wired at `11-overview:34`; re-scoped). F4 = `19g` attribution drift (names routing skills, not the honoring chapter). Exhaustive per-attribution check for the 21 soft instruction fields → **Session 2**. Concurrent `/explain` feature added a 42nd field (`Explanation style`) post-baseline — flagged out of scope. PF1 caught both the section prose and several of my own mid-session fabrications (corrected by reading actual files). Mutations confined to the section file + `FIXES.md` by authorship (`git diff 719e27c`). Pointer advanced to `audit-04-skill-triggers` (stub — `/prep` required first).

### 2026-05-29
- Audit-04 `/prep` ran. Section file `docs/plans/audit-04-skill-triggers.md` elaborated from the 56-line stub into a fully-detailed Session 1 entry (extract all 23 op-* descriptions → 13b-rubric verdict + overlap matrix + description↔body drift → proposed revisions in Findings + cost-gated re-run recommendation). Three decisions locked: **(1) coverage = all 23** (count corrected from the stub's stale "22"; Section 0's `op-approach` is repo-only / not installed this session — a live audit-02 F1 staleness instance; benchmark covered only 18). **(2) benchmark re-run = no-spend by default** with an authorize-branch at step 5 — the `claude -p` routing-skill confound (benchmark README § Caveats) means a naive re-run reproduces the prior 0–20% TP artifact (FP is the only reliable signal). **(3) harmful-overlap test** — single phrase matches both descriptions + no disambiguation = harmful; explicit boundary or ambient-party = benign. Cross-section notes carried forward: audit-01 PF1 methodology (load-bearing), audit-02 F1 staleness + A1 routing-accuracy reframe (load-bearing — this section audits the spine's post-LC1 headline value-add), audit-03 "nothing for audit-04" re field→description, + a concurrent-`/explain` coherence caveat (op-welcome/op-onboard have uncommitted description edits in the working tree). Seeded findings-to-expect: 5-skill eval-set gap + harness confound → audit-06; any router-shape drift → audit-05. No `chapters/`/`skills/core/`/`tests/`/code/template edits; writes confined to the section file + this PROGRESS pointer refresh. Pre-flight rule 1 satisfied for audit-04.
- Audit-04-skill-triggers Session 1 done. 11 findings (1 blocking, 8 drift, 2 polish) across all 23 op-* descriptions. Blocking F1 = `op-workflow`↔`op-prepare` planning-misroute (collide on "new project / scope this out", no phrase-visible boundary). Overlap matrix: 7 harmful + 6 benign. Re-run recommendation = **NO-SPEND** (15/18 benchmarked descriptions byte-identical since 2026-05-27, git-verified; only op-onboard/op-persistence/op-signaling changed post-run; the 5 uncovered need eval-sets + the harness needs a `claude -p` confound fix → audit-06). Escalated to **`FIXES.md` A15** (F1 + F2–F9; F10–F11 polish stay in section file). Drift body-confirmed (all 23 bodies read, PF1). Read-only honored by authorship (concurrent `/explain` edits to `19g` + `explain.md` are not audit-04's; the staged audit-03 close is not audit-04's). Pointer → `audit-05-self-discipline` (stub — needs `/prep`).
- Audit-05 `/prep` ran (2026-05-29). Stub elaborated into a fully-detailed Session 1 entry (12-rule scorecard + 18a–18h dogfooding + PF1–PF5 confirmation + magic-number grep + claim-#1 framing check). **Consolidation mandate** set: cite audits 01–04's rule-relevant findings (A13 rule 4 / A15+audit-01 rule 6 / A2.5 rule 10 / audit-04+audit-01 rule 11), don't re-discover. Read-only widened to `global/` (PF2–PF5 target it). `A16` reserved for the four audit-infra bugs; the claim-#1 finding extends A1. Single-session shape with a documented split. Writes confined to the section file + this pointer refresh. Pre-flight rule 1 satisfied for audit-05.
- Audit-05-self-discipline Session 1 done. 12-rule scorecard (3 PASS / 6 PARTIAL / 1 FAIL = INDEX-11g / 2 PASS-flag); 18a–18h dogfooded (**18h committed** = CLAUDE.md drift); PF1–PF5 (PF2/PF3/PF5 → A16, PF4 downgraded — instance self-resolved, PF1 wired). **Two fresh findings:** rule-12/04a (model-ID dup vs rule wording + `CHANGELOG:106` overclaim + Opus 4.7-vs-4.8 rot → A16.5) + claim-#1 in `CLAUDE.md:42/44` (→ A1 extended). Consolidations cited (A13 + `CLAUDE.md:83`; A15 rule-6 framing answer; A2.5). FIXES: A16 opened + A1 extended. Read-only verified by authorship. Pointer → `audit-06-tests-docs` (stub — needs `/prep`; the final audit).
- Audit-06 `/prep` ran (2026-05-29). Stub elaborated into a fully-detailed Session 1 entry — 3 mechanical sweeps (test-coverage map + untested paths + count reconcile; cross-ref + anchor validity; archive/freshness) + the **verify-command synthesis capstone** (consolidate A12 `/profile verify` + audit-04 `/trigger verify` + audit-05 `/discipline verify`; frame "no automated self-lint → silent inter-audit drift" as the meta-gap audits 03/04/05 each independently hit). Read-only widened to `tests/`. `A17` reserved (coverage gaps + the verify-command recommendation; synthesis extends A12). Cross-section notes carried forward from **all five** prior audits. Fresh seed: test-count claim drift (`CLAUDE.md` "~57/6 sub-suites" vs "6 suites/~65" vs run.sh's 7). Cleared a stray 16:22 PF2 heartbeat from the stub. Single-section shape with a documented split (Session 2 = synthesis, only if Session 1 balloons). Writes confined to the section file + this PROGRESS refresh. Pre-flight rule 1 satisfied for audit-06.
- Audit-06-tests-docs Session 1 — **done** (no split — synthesis completed inline). 7 findings (0 blocking, 6 drift, 1 bug = **F7** uninstall removes 1-of-6 hooks + false `CHANGELOG` H5 claim). **A17 opened** (F1 untested notify hook / F3 CLAUDE.md test-counts → 7 suites·131 cases / F4 install.sh "22" vs 23 + `test-dry-run.sh:121` enforces it / F5 `CHANGELOG:165` broken link / F7); **A12 extended** = the `/verify [refs|counts|profile|triggers|discipline]` family (the six-audit capstone — no self-lint → silent drift, proven by this session's own sweep no-op'ing twice under zsh); **A13 extended** (F6 = 3rd `## Hook tuning` broken anchor, `19b:67`). Cited not re-opened: A16.1 (F2 append-test gap), A16.5 (model rot + `CHANGELOG:106` overclaim). Read-only verified (`git diff 3edfb87`). **🔓 All-six gate LIFTED — audit phase complete.** Pointer → `apply-01` (needs `/prep`; first cluster by FIXES severity).
- **apply-01 `/prep` ran (2026-05-29) — first apply section.** Chose **A1** (token-efficiency reframe; top "Blocking — before LC5") per FIXES severity. Drafted `docs/plans/apply-01-efficiency-claim.md` — 2 sessions (S1: publish LC1 in `REPORT.md` + archive 114 `.err` + version-guard `run.sh`, no API spend; S2: reframe `CLAUDE.md:42/44` + sweep README/landing/EXPLAINER + compress & close A1). Prep-time disk checks fixed 3 stale FIXES assumptions: `REPORT.md` exists (stub, not absent); live reframe surface is **CLAUDE.md only** (sweep found no token-savings claim in README/landing/EXPLAINER); 57+57 result pairs + 114 `.err` need reconciliation vs audit-02's n=11. Filename omits "token" to dodge the `Read(**/*token*)` deny trap (A16.3). Plan-only session — no code/doc edits. Pointer → apply-01 Session 1.
- **apply-01 Session 1 done (2026-05-29).** Published `benchmarks/tokens/REPORT.md` — disk-true LC1 headline **+76.6 % input / +28.6 % cost / −31.5 % output**, n=11 paired prompts, reconciles with audit-02 F0 to the decimal (F0's +75.9 % = per-prompt mean-of-means; this = call-weighted mean). Resolved the 57/57-vs-11 question (11 paired + 8 ON-only from the mid-run CLI upgrade). Archived 114 `.err` + 27 failed-OFF + 24 ON-only raw + full 171-record log to `results/archived-pre-publish/`; filtered live `results.jsonl` to 63 paired-ok. Hardened `run.sh` (`assert_claude_unchanged` version+mtime guard; unit-tested 0/3/3). No API spend; `tests/run.sh` green (7 suites). Tracked mutations: `REPORT.md` + `run.sh` (results/ reorg gitignored). Surfaced an out-of-scope `aggregate.py` Totals-row finding (sum-based, over-reports on unequal n) for a future FIXES item. Pointer → apply-01 Session 2 (doc-reframe + A1 close).
- apply-01 Session 2 done (2026-05-29) — **docs-only, no API spend.** Reframed claim #1 at `CLAUDE.md:42/44` ("prove the win"/"cheaper" → bounded per-load cost + routing accuracy, cites REPORT.md); swept README:92/landing/EXPLAINER (already safe — no edit, confirms audit-05); compressed + **resolved** `FIXES.md` A1 (two narrative essays → one action entry, rule 1) + marked LC1 done + dropped A1 from FIXES/CHANGELOG residue lists; `CHANGELOG [Unreleased]` Changed bullet; `tests/run.sh` green (7 suites). Pending-notes reviewed → 3 dismissed (PF2 noise; `aggregate.py` Totals finding already in Cross-session notes + Blockers). Session 1 committed separately (`3ee2c92`). **apply-01 complete** → pointer to `apply-02` (needs `/prep`).
- **`/prep apply-02` ran (2026-05-29) — second apply section.** Chose **A15** (skill-trigger accuracy) per FIXES severity, confirmed with the user against the alternatives (A16 audit-infra bugs / A17 install+test+docs / A2 personalization). Drafted `docs/plans/apply-02-skill-triggers.md` — **2 sessions** (S1: fix the 7 harmful op-* trigger overlaps incl. blocking A15.1, 8 `skills/core/*/SKILL.md` `description:` edits; S2: 2 drift cleanups F4/F7 + compress & close A15 — both no API spend). Disk-grounded at prep: (1) op-workflow's F1 fix lands in `description:` (L3) + its existing `## Sibling skills` section (L47–50; audit-04's "SKILL.md:48–50" citation is accurate — the section lists op-collaboration-modes + op-brownfield, omits op-prepare); (2) `tests/run.sh` does **not** test routing (it skips `tests/skill-triggers/` per run.sh:13; that benchmark is API-gated + **deferred to audit-06's `claude -p` confound fix**) — apply-02 uses the audit-04 overlap matrix as the routing proxy, does not re-run the benchmark. Plan-only session — no code/doc edits. Pointer → apply-02 Session 1.

### 2026-05-30
- **apply-02 Session 1 done.** Applied audit-04's 7 harmful-overlap clauses (F1 blocking A15.1 + F2/F3/F5/F6/F8/F9) to 8 `skills/core/*/SKILL.md` descriptions + op-workflow's `## Sibling skills` op-prepare bullet. 9/9 boundary greps PASS; `tests/run.sh` 7 suites green; overlap-matrix re-check clean (op-workflow↔op-approach checked, no new collision); API routing benchmark **not** run (deferred). Eval-set sanity check (no spend) flagged `op-hooks.json` L4 now stale post-F2 (→ S2 deferred-benchmark note; `tests/` untouched). F3 applied bidirectionally (beyond audit-04 verbatim). `git diff --stat`: only the 8 SKILL.md (9 ins/8 del), no body/`chapters/`/`tests/`. Pointer → apply-02 Session 2 (F4/F7 + close A15).
- **apply-02 Session 2 done — apply-02 section COMPLETE.** F4 (op-curate-nudge description leads with the default-off bucket-loop gate) + F7 (op-curate description trimmed of apply/reject procedure; body's Flow steps + `Read-before-write` section retain all mechanics, grep-confirmed) applied; overlap re-check clean (F4 narrows nudge's firing; F7 left curate's triggers unchanged; `/curate` FP covered by S1's F5 carve-out). **F10/F11 → LEFT** in audit-04 as polish (user-confirmed, not folded — no-bundling). **`FIXES.md` A15 → ✅ RESOLVED** (F1–F9 essay → one action-shaped bullet + audit-04 § Findings pointer; deferred-benchmark note recorded incl. `op-hooks.json` L4 stale-eval to fix pre-re-run). `CHANGELOG [Unreleased]` Changed bullet. `tests/run.sh` 7 suites/0 fail; concrete S2 checks pass; benchmark **not** run; no `tests/skill-triggers/` changes. `git diff` scope: 2 SKILL.md *descriptions* + `FIXES.md` + `CHANGELOG.md` + plan/progress docs — no bodies / `chapters/` / `tests/`. Pointer → `apply-03` (needs `/prep`; next cluster by severity — A16 / A17 / A2 / A12).

---

## Notes

- For the project plan (sections + dependencies), see `docs/PROJECT_PLAN.md`.
- For per-section detail (session entries with build steps + verify checks), see `docs/plans/<section-name>.md`.
- For the open work queue (blocking findings + LC1–LC6, BA3, A1–A12), see `FIXES.md`.
- For shipped work history, see `CHANGELOG.md`.
