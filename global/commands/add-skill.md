---
description: Add a new skill to the personal bucket (~/.claude-spine/bucket/skills/). Walks you through naming, trigger description, and body, then auto-updates bucket/INDEX.md.
---

Invoke the `op-add-skill` skill.

Read `~/.claude-spine/skills/core/op-add-skill/SKILL.md` and follow it. Before gathering inputs, apply the gate it describes — refuse politely if the pattern isn't reach-for-it-3+-times-worthy.

After writing the file, summarize what landed and remind the user that `op-bucket-router` will pick it up automatically next time the trigger fires.
