# Global CLAUDE.md — {{YOUR NAME OR BUILD BOX}}

> Thin always-on stub, TypeScript + Next.js + Supabase flavor. The eight rules below stay in context every session — they're the ones I most regret you forgetting.
> Heavier stack discipline (how to work, decisions, first-touch, workflow, signaling, code output, verification, debugging, stop conditions, stack defaults, full security non-negotiables, project conventions) lives in the `op-stack-flavor` skill, loaded on-demand when stack-relevant work or questions show up.
> Copy to `~/.claude/CLAUDE.md` and fill the `{{placeholders}}`. Loaded into every Claude Code session on this machine.
> Project-level `.claude/CLAUDE.md` can add to these rules (never weaken security).

{{ONE OR TWO LINES ABOUT YOU AND THIS MACHINE.}}
Example: "Solo founder. This machine builds {{type of work — e.g. client websites and MVP apps}}. Focus: ship working products fast for real users."

## ⚡ Always-on — read first, every session

These are the ones I most often regret you forgetting. If something below conflicts with this block, this block wins.

1. **Proposal mode ≠ build mode.** When I'm exploring ("should we...", "what about...", showing screenshots, citing things I read) — stay in reviewer. Don't write files until I say "build it" / "go ahead" / "yes, do that." Silence and follow-up questions are not approval.
2. {{Q4}}
3. **Don't invent.** File paths, API names, library features, version-specific syntax — if you're not certain, say so and verify. A checked answer beats a confident wrong one.
4. **"Done" requires verification.** UI rendered. Route hit. Migration applied. RLS tested from two sessions. If you can't run it, say so — don't claim it works.
5. **One session = one feature.** Fresh terminal between features. Context is a budget, not bandwidth.
6. **Security is non-negotiable** (full list in `op-stack-flavor` — load it before shipping anything user-facing). Quick filter: privileged keys never reach the client. RLS on every table from day 1. `.env*` gitignored. `catch (e) {}` is banned.
7. **Slash commands first.** `/code-review`, `/verify`, `/security-review`, `/ultrareview` beat ad-hoc work.
8. **Don't invent project docs.** Standard project docs (CLAUDE.md, PROGRESS.md, DECISIONS.md, FEATURES.md, ARCHITECTURE.md, PROJECT_BRIEF.md, SMOKE_TESTS.md, DEPLOY.md, SESSION_STARTER.md) have templates at `~/.claude-spine/templates/`. Copy and adapt — never invent `TODO.md` / `NOTES.md` / `ROADMAP.md` alternatives.

## Where the rest lives

- **`op-stack-flavor` skill** — how-to-work / decisions / first-touch / workflow / signaling / code output / verification / debugging / stop conditions / stack defaults / full security / project conventions. Fires on stack-relevant work, security questions, or "is this done?" moments.
- **Universal claude-spine skills** (`op-*`) — workflow, prompting, signaling, recovery, tools, anti-patterns, persistence. Each routes to one atomic file under `~/.claude-spine/chapters/`. Browse `~/.claude-spine/INDEX.md` if no skill fires and you need to find the right chapter by hand.
- **Personal profile** — `~/.claude/claude-spine-profile.md` (created by `/onboard`). User-specific calibration overrides spine defaults.

## Override hierarchy

1. Project-level `.claude/CLAUDE.md` (most specific).
2. Workspace-level `.claude/CLAUDE.md` (e.g. `/Dev/`).
3. `~/.claude/claude-spine-profile.md` (this user's calibration).
4. The `op-stack-flavor` skill (stack-specific discipline).
5. This file (the always-on essentials).

More specific wins — except security rules, which apply at every level.
